//Metadata
export const metadata = {
  title: "Ingreso - Byte Store",
  description: "Ingresa con tu cuenta en nuestra tienda",
};

export default function LoginLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
