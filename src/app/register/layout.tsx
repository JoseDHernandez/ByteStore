//Metadata
export const metadata = {
  title: "Registro - Byte Store",
  description: "Regístrate en nuestra tienda",
};

export default function RegisterLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
