export type User = {
  id: string;
  name: string;
  role: number;
  token: string;
};

export type UserRegister = {
  id: string;
  name: string;
  email_address: string;
  physical_address: string;
};
